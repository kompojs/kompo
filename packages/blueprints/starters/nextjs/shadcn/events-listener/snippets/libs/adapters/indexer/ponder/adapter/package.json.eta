{
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "codegen": "ponder codegen",
    "build": "mkdir -p dist && echo 'Ponder does not require a build step' > dist/.keep"
  },
  "dependencies": {
    "@ponder/core": "^0.7.17",
    "@<%= it.scope%>/events": "workspace:*",
    "@<%= it.scope%>/config": "workspace:*",
    "@<%= it.scope%>/event-cache-redis": "workspace:*",
    "@<%= it.scope%>/event-queue-bullmq": "workspace:*",
    "@<%= it.scope%>/event-rpc-viem": "workspace:*",
    "hono": "^4.0.0",
    "viem": "catalog:"
  }
}
