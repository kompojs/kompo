import { Database } from 'bun:sqlite'
import { drizzle } from 'drizzle-orm/bun-sqlite'

export function createBunSqliteClient(filename: string) {
  const sqlite = new Database(filename)
  return drizzle(sqlite)
}
