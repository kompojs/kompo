name: <%= it.scope %>

services:
  <%= it.ormDriver %>-<%= it.alias %>:
    image: node:20
    container_name: <%= it.ormDriver %>-<%= it.alias %>
    working_dir: /app
    volumes:
      - ./data:/app/data
    command: tail -f /dev/null
