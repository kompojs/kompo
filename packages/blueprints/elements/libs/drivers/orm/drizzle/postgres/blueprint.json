{
  "sharedDriver": "postgres",
  "env": {
    "DATABASE_URL": {
      "description": "PostgreSQL connection string",
      "validation": "z.string().url()",
      "side": "server",
      "default": "postgres://user:password@localhost:5432/main"
    }
  },
  "id": "postgres",
  "name": "postgres"
}
