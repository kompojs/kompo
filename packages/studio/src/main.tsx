/**
 * Kompo Studio — Standalone dev entry point.
 *
 * In standalone mode (pnpm dev:studio), the toolbar IS the entire UI.
 * The injector auto-mounts the toolbar panel at the bottom of the page.
 */
import './client/injector'
